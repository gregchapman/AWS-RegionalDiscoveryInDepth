service: kms
client: kms
display_name: KMS Keys

operations:
  - name: KMS Keys
    method: list_keys
    paginator: true
    result_key: Keys
    id_field: KeyId
    name_field: KeyId
    key_prefix: kms
    dr_note: "KMS keys are region-specific. Create equivalent keys in DR with same aliases."
    config_fields:
      - KeyId
      - KeyArn
